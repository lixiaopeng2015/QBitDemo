<?xml version="1.0" encoding="UTF-8" ?>
    <!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sampson.mapper.SiteMapper">

    <select id="getCrawlerSites" resultType = "site">
      SELECT id,crawl_site crawlSite,create_time createTime FROM hdx_sites
    </select>
</mapper>